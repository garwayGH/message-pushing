<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viatom.messagepushing.mapper.UserMapper">

    <select id="getUserByPhone" parameterType="java.lang.String" resultType="com.viatom.messagepushing.pojo.User">
        select * from user where phone = #{phone}
    </select>

</mapper>